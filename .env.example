# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# Database (if using Supabase or similar)
# DATABASE_URL=your_database_url_here

# Authentication (if using Auth0, Firebase, etc.)
# AUTH_SECRET=your_auth_secret_here
